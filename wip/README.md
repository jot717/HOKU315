# WIP quarantine

Unfinished systems **must not** live in active product roots.  
Promote to `product/` or `fox_quiz/` only after governance sprint + `ACTIVE_SURFACE_MAP` update.

| Folder | Contents |
|--------|----------|
| `app_shell/` | App shell replacement spec + backlog |
| `experimental_match/` | `match_engine.py`, `match_flow.py` drafts |
| `workflow/` | CI process enforcement draft |
| `prototypes/` | Quarantined regression prototypes |
