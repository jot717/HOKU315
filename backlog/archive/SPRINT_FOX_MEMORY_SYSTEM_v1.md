# SPRINT — FOX MEMORY SYSTEM v1

## DELIVERABLES

* `fox_memory_store` + `fox_memory_engine`
* `fox_memory_card` UI
* `AppState` integration (`fox_memory_note`, `recurring_pattern`)
* insight panel placement (under fox avatar)

## DONE CRITERIA

* After a successful insight run, disk + UI show a **guardian memory note**
* Reloading `/insight` still surfaces the last remembered tone (from JSON)
* No new external services or schema migrations

## STATUS

ACTIVE
