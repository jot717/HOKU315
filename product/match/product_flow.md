# MATCH FLOW v1

```
[User opens app]
        ↓
[Click Match Button]
        ↓
[Build simple profile comparison]
        ↓
[Compute match score]
        ↓
IF score >= threshold:
    → create match record
    → show match UI
    → unlock entry enabled
ELSE:
    → show no match state
```
